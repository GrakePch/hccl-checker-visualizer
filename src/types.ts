export type TestStats = {
  rankCount: number;
  streamCount: number;
  taskCount: number;
};

export type WaitArrow = {
  id: string;
  startX: number;
  startY: number;
  endX: number;
  endY: number;
};
